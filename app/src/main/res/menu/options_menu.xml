<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/bg_menu_item"
        android:icon="@drawable/ic_menu_icon"
        android:title="Background"
        app:showAsAction="ifRoom">
        <menu>
            <item android:id="@+id/bg_sub_white"
                android:title="White"/>
            <item android:id="@+id/bg_sub_gray"
                android:title="Gray"/>
            <item android:id="@+id/bg_sub_dark"
                android:title="Dark"/>
        </menu>
    </item>
    <item android:id="@+id/stop_item"
        android:title="Finish game"
        app:showAsAction="never"
        />
    <item android:id="@+id/quit_item"
        android:title="Quit game"
        app:showAsAction="never"
        />
</menu>